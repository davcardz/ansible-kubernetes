- name: kube test
  hosts: localhost
  connection: local
  tasks:

    - name: Prova conexió
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Service
        name: web
        namespace: awx
      register: web_service


