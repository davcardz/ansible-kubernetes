- name: kube test
  hosts: localhost
  connection: local
  tasks:

    - name: Prova conexió
      kubernetes.core.k8s:
        api_version: v1
        kind: Service
        name: web
        namespace: awx
        namespace: default
      register: web_service


